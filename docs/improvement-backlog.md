# 블로그 개선 목록

| ID | 우선순위 | 문제와 근거 | 예상 효과 | 상태 |
|---|---|---|---|---|
| SEARCH-001 | 높음 | 검색어를 순차 치환하여 생성한 mark 태그와 HTML 엔티티까지 다시 치환함. `AI a`, `& <`에서 재현 | 검색 결과의 원문과 강조 표시 보존 | 완료: 2026-09-07, 0dc688a 배포·공개 검색 검증 성공 |
| CONTENT-001 | 보통 | 9월 7일 교정은 최신 10개 및 일부 4월 글 중심. 전체 141편의 출처·코드 검증은 미완료 | 기존 콘텐츠 신뢰성 개선 | 일요일에 검토 범위를 기록하며 순차 점검 |
| SEARCH-003 | 보통 | 390px 공개 검색 `AI a`에서 결과 카드 오른쪽이 약 394px까지 확장되어 잘림. 기존 배포 84884d8에서도 확인, 결과 124개와 강조 동작 정상 | 작은 화면의 결과 카드 여백 보존 | 완료: 2026-09-10, c7232c5 배포 후 320/390/768/1280px 실제·긴 문자열 검사 통과 |
| ANALYTICS-001 | 보통 | 현재 실행에서 이용 가능한 유입·조회 데이터 연결은 확인하지 않음 | 실제 독자 수요에 따른 우선순위 결정 | 월간 점검 시 연결 상태 확인, 수치 추정 금지 |


## 고도화 후보 — 2026-09-09 조사

유지보수 항목과 별도로 관리한다. 예상 효과는 가설이며 실제 유입·조회 성과를 측정한 수치가 아니다. 아래 후보는 기존 코드와 공개 화면에서 기능 유무를 확인했다. 월·수·금에 독자 가치와 구현 비용을 비교해 하나를 배포한다.

### READ-001 · 글 목차와 문단 링크
- 우선순위: 높음 / 상태: 완료 — 2026-09-09, `d5a8761` 배포·공개 기능 검증 성공.
- 근거·현재 경험: `app/posts/[slug]/page.tsx`는 본문만 렌더링하고 `src/lib/posts.ts`는 제목 ID를 생성하지 않는다. 공개 최신 글의 소제목 5개 모두 ID가 없고 목차도 없다.
- 독자·개선 후 경험: 긴 글에서 필요한 부분을 찾는 독자가 목차로 바로 이동하고 문단 URL을 공유한다.
- 예상 효과: 탐색에 필요한 스크롤 감소, 특정 예제·설명 재방문 편의.
- 수용 기준: h2/h3로 목차 생성, 한글·중복 제목에 고유 링크, 코드 속 제목·각주 제외, 모바일 접기·펼치기와 키보드 이동, 직접 문단 URL 재방문. 짧은 글은 불필요한 목차를 표시하지 않는다.
- 의존성: 없음. 기존 공개 slug와 날짜 보존.

### READ-002 · 코드 블록 복사
- 우선순위: 높음 / 상태: 완료 — 2026-09-11, `b46d5bd` 배포·공개 코드 복사 검증 성공.
- 근거·현재 경험: 본문은 정적 HTML이며 복사 버튼이 없다. 9월 2일 글의 YAML 예제를 수동 선택해야 한다.
- 독자·개선 후 경험: 코드를 따라 하는 독자가 블록 전체를 한 번에 복사한다.
- 예상 효과: 모바일 선택 부담과 부분 복사 실수 감소.
- 수용 기준: 코드 원문·개행 보존, 블록별 복사 성공/실패 피드백, 키보드 조작, Clipboard API 불가 상황 처리, 코드 없는 글 영향 없음.
- 의존성: 없음. 브라우저 권한 실패 처리 필요.
- 9월 10일 구현 범위 구체화: 기존 `pre > code` 원문을 대상으로 블록별 복사 버튼을 추가한다. 목차 및 서버 HTML 정제를 유지하고, 추가 패키지 없이 필요한 버튼 상호작용만 클라이언트에서 처리한다. JavaScript가 없어도 코드 원문을 읽고 수동 선택할 수 있어야 한다.
- 검증 시나리오: 9월 2일 YAML 단일 블록과 3월 18일 글의 여러 코드 블록에서 각각 다른 원문이 정확히 복사되는지 확인한다. 한글·따옴표·꺾쇠·들여쓰기·마지막 개행을 보존한다. 버튼 문구가 복사 본문에 섞이지 않아야 한다.
- 실패·접근성 기준: Clipboard API 미지원/권한 거절 시 성공으로 표시하지 않고 수동 선택 안내를 제공한다. Enter·Space 조작, 스크린리더 상태 알림, 연속 복사와 타이머 정리, 코드 없는 글 및 390px 화면을 확인한다. 목차 링크와 코드 가로 스크롤을 방해하지 않는다.

### DISCOVER-001 · 시리즈 밖 관련 글 연결
- 우선순위: 높음 / 상태: 완료 — 2026-09-21, 7291f63 공개 배포·기능 검증 성공.
- 근거·현재 경험: 상세 하단은 `editorial.ts`의 3개 시리즈 가입 글만 연결한다. 시리즈 밖 글에는 관련 글 추천이 없다.
- 독자·개선 후 경험: 글을 끝까지 읽은 독자가 같은 세부 주제의 다음 글을 선택한다.
- 예상 효과: 141편 중 시리즈 밖 콘텐츠 발견 기회 확대.
- 수용 기준: 자기 자신·초안·중복 제외, 태그 근거 표시, 넓은 AI 태그만의 우연한 추천 제한, 최대 3편, 관련 후보 없으면 숨김, 기존 시리즈 탐색 보존.
- 의존성: 없음. 추천 규칙과 대표 글의 실제 관련성 확인 필요.
- 9월 12일 조사: 검색 인덱스의 공개 글은 141편, 정규화 태그는 469종이다. `AI Agents` 49편, `AI` 40편, `Architecture` 39편, `MCP` 38편이다. 빈도만으로 제외하면 구체적 기술인 MCP도 제외되므로 범용 태그 목록을 따로 관리한다. 이 수치는 콘텐츠 분포이며 독자 이용 지표가 아니다.
- 구현 초안: 같은 태그의 중복을 제거하고 `AI`, `AI Agents`, `Architecture`, `Agentic AI`, `2026`, `2026 Trends`, `trends`, `Software Engineering`만의 겹침은 추천 근거로 사용하지 않는다. 최소 하나의 나머지 공통 태그가 있어야 한다. 공통 태그별 `log((공개 글 수 + 1) / (태그 포함 글 수 + 1))` 합으로 순위를 정하고 동점은 발행일 내림차순·slug 순으로 고정한다. 구현 시 대표 결과를 다시 검토하며 이 기준은 아직 서비스에 적용하지 않았다.
- 배치·중복 기준: 본문 및 기존 시리즈 읽기 순서 다음에 최대 3개를 서버에서 렌더링한다. 현재 글과 같은 시리즈 목록에 이미 노출된 글, 자기 자신·초안·중복 slug는 제외한다. 후보가 없으면 섹션을 숨긴다. 공통 태그를 추천 이유로 보여주고 개인화 추천이나 인기 순위라고 표현하지 않는다.
- 실제 검증 사례: 시리즈 밖 `2026-08-29-proactive-update`에서 `2026-09-02-proactive-update`, `2026-08-26-proactive-update`, `2026-08-09-proactive-update`는 Cloud Native 공통 주제로 연결 가능한 후보이다. 같은 글과 2026 Trends만 겹치는 8월 25일 글은 제외한다. `2026-03-18-proactive-update`와 `2026-03-23-proactive-update`는 Frontend·MCP가 함께 겹치는 후보이다.
- 수용 검사: 범용 태그만 겹침→0개, 구체 태그 겹침→이유 표시, 같은 시리즈 중복 제외, 태그 별칭 정규화, 입력 순서가 달라도 동일 결과, 동점·0~2개 후보·빈 태그·초안·중복 slug, 최대 3개. 390px/데스크톱에서 카드 클릭→해당 글, 목차·코드 복사 회귀를 확인한다.

### SEARCH-002 · 검색 조건 URL 보존
- 우선순위: 보통 / 상태: 완료 — 2026-09-23, 7ab97b5 공개 기능 검증 성공.
- 근거·현재 경험: `SearchClient.tsx`는 최초 q만 URL에서 읽고 이후 검색어·태그·정렬은 상태에만 저장한다. 태그 필터 자체는 이미 있다.
- 독자·개선 후 경험: 검색한 조건을 링크로 공유하거나 새로고침해 같은 결과를 본다.
- 예상 효과: 반복 탐색과 결과 공유 편의.
- 수용 기준: q/tag/sort URL 왕복, 한글·특수문자 보존, 뒤로/앞으로 이동, 잘못된 조건 기본값, 입력마다 과도한 이력 생성 방지.
- 의존성: 없음. 기존 검색 강조 회귀 검사 포함.

### READ-003 · 글의 읽던 문단 이어보기
- 우선순위: 보통 / 상태: 준비.
- 근거·현재 경험: 상세 페이지에 읽기 위치 저장이 없어 재방문 시 처음부터 찾는다. READ-001의 문단 ID를 재사용할 수 있다.
- 독자·개선 후 경험: 긴 글을 나눠 읽는 독자가 같은 기기에서 읽던 문단으로 돌아간다.
- 예상 효과: 재방문 탐색 부담 감소.
- 수용 기준: 자동 강제 이동 없이 이어보기 선택 제공, 명시적 URL fragment 우선, 저장 거부/실패 시 정상 읽기, 로컬 기록 삭제, 변경·삭제된 문단 처리.
- 의존성: READ-001 공개 완료. 기기 내 저장임을 UI에 명확히 설명.

### CONTENT-002 · 검증 가능한 파이프라인 실습 보강
- 우선순위: 보통 / 상태: 조사 필요.
- 근거·현재 경험: 9월 2일 글의 orchestration YAML은 개념 예시로 실행 도구·명령·예상 결과가 없다.
- 독자·개선 후 경험: 아키텍처를 배우는 개발자가 개념 예시와 실행 가능한 실습을 구분하고 최소 예제를 따라 한다.
- 예상 효과: 개념 설명에서 검증 가능한 학습 경험으로 확대.
- 수용 기준: 기존 글 URL·날짜 보존, 개념 예시 명시, 공식 문서 출처, 독립 환경에서 재현한 실행 명령·예상 결과·실패 사례 제공. 실제 운영 경험·성과를 창작하지 않는다.
- 의존성: 사용할 실행 도구 선정과 공식 문서 조사, 자격증명 없이 재현 가능한 예제 확보.


### DISCOVER-002 · 태그 목록에서 주제 찾기
- 우선순위: 보통 / 상태: 준비.
- 근거·현재 경험: `app/tags/page.tsx`는 전체 태그를 목록으로 출력하고 이름 검색 입력이 없다. 원하는 세부 태그를 찾으려면 긴 목록을 훑어야 한다. 통합 검색의 결과 필터와는 별개인 태그 아카이브 탐색 흐름이다.
- 독자·개선 후 경험: 관심 주제로 진입하는 독자가 태그 이름 일부를 입력해 후보와 글 수를 확인하고 해당 아카이브로 이동한다.
- 예상 효과: 긴 태그 목록의 탐색 부담 감소.
- 수용 기준: 한글·대소문자·공백 포함 이름 검색, 결과 수·빈 결과·초기화, 키보드 조작, 모바일 너비 보존, 기존 정적 링크와 별칭 canonical 유지, JavaScript 없이 전체 목록 접근 가능.
- 의존성: 없음. 기존 태그명과 공개 URL은 변경하지 않는다.


## 2026-09-20 검토 결과와 다음 주 실행 순서

미완료 고도화는 DISCOVER-001, SEARCH-002, READ-003, CONTENT-002, DISCOVER-002의 5개이다. 원격 main과 공개 build-info는 9월 12일 소스 7364d65이며, 9월 14~20일 신규 기능 배포는 확인되지 않았다. 아래는 계획이며 완료 성과가 아니다.

1. **9월 21일 월요일 — DISCOVER-001**: 시리즈 밖 글에서 다음 글로 이동하도록 관련 글 최대 3편을 제공한다. 9월 12일에 기록한 구체 태그·동점·중복 제외 기준과 대표 글 클릭 흐름을 검증한다. 의존성 없음.
2. **9월 23일 수요일 — SEARCH-002**: 검색 조건을 공유·복원할 수 있도록 q/tag/sort를 URL과 동기화한다. 현재 SearchClient는 초기 q만 읽으며 태그·정렬은 로컬 상태다. 한글·특수문자, 새로고침, 뒤로/앞으로, 잘못된 값, 연속 입력 이력과 검색 강조 회귀를 검증한다. 의존성 없음.
3. **9월 25일 금요일 — DISCOVER-002**: 긴 태그 목록에서 이름 일부로 주제를 찾도록 검색·결과 수·초기화를 제공한다. 현재 app/tags/page.tsx에는 전체 정적 목록만 있다. 키보드·390px·빈 결과·한글·공백과 JS 없는 전체 목록 접근을 검증한다. 의존성 없음.

### CONTENT-001 교정 대기 항목

- 우선순위 높음: 9월 2일 글의 Gartner 95% 예측 연도는 2026년이 아니라 2025년이다. 2021년 발표한 예측임을 명시하고 실측 성과와 구분한다. 비용 절감·속도 향상을 단정하는 문장은 근거를 추가하거나 조건부 설명으로 교정한다. YAML은 특정 실행 도구의 설정이 아닌 개념 예시임을 명시한다.
- 우선순위 높음: 8월 29일 Go 예제는 출력 채널이 막히면 취소 신호를 처리하지 못하고 닫힌 입력 채널의 zero value를 처리한다. 입력의 ok 확인, 출력 select 취소 처리, 채널 소유권·종료 규약을 포함한 독립 실행 예제와 재현 검사가 필요하다. 정의되지 않은 타입·함수도 보강한다.
- 같은 글의 mTLS 필수·메시지 큐 부족·고성능 단정은 적용 조건과 근거를 보강한다. 현재 코드는 성능 측정이나 스트림 처리 시스템의 보장을 보여주지 않는다.
- 이번 회차는 검토와 재현만 수행했으며 위 교정은 공개 글에 아직 반영하지 않았다. 단순 교정을 고도화 성과로 세지 않는다. CONTENT-002의 실행 가능한 실습 보강과도 구분한다.


### DISCOVER-003 · 시리즈에서 읽은 글 표시
- 우선순위: 낮음 / 상태: 준비.
- 근거·현재 경험: app/series/page.tsx와 글 하단 시리즈에는 읽기 순서만 있고 읽은 글 표시나 기록 초기화가 없다.
- 대상 독자·개선 후 경험: 여러 번에 나누어 시리즈를 읽는 독자가 직접 읽음 표시를 하고 남은 글을 확인한다.
- 예상 효과: 같은 시리즈 재방문 시 다음 학습 단계 선택 편의. 효과는 가설이며 이용 통계가 아니다.
- 수용 기준: 명시적인 읽음/해제, 기기 내 저장 안내, 기록 삭제, 저장 불가 시 기존 링크 정상, 키보드 조작, JS 없는 읽기 순서 유지. 자동으로 완독을 추정하지 않는다.
- 의존성: 없음. READ-003 문단 위치 저장과 별개이며 하나의 회차에는 시리즈 완료 표시만 구현한다.

### 2026-09-21 DISCOVER-001 구현 검증
- 세부 태그 가중치·결정적 정렬·최대 3편·시리즈 제외를 구현했다. 관련 후보 없으면 섹션을 숨기고 정적 HTML에 링크와 공통 주제를 제공한다.
- 상태: 완료 — 7291f63 공개 추천·클릭 이동·시리즈 중복 제외 검증 통과. SEARCH-002, READ-003, CONTENT-002, DISCOVER-002, DISCOVER-003의 미완료 5개를 유지한다.


### 2026-09-22 SEARCH-002 구현 기준 구체화

- 공개 화면 재현: `/search/?q=AI&tag=Cloud%20Native&sort=new`로 들어와도 현재 코드는 q만 초기화하고 태그·정렬은 기본값으로 시작한다. 이후 cloud 입력·Cloud Native 선택·최신순 선택을 해도 URL의 q=AI가 남는다. 입력·선택 UI 자체의 장애가 아닌 조건 공유·복원 기능의 부재다.
- URL 계약: q는 입력 문자열을 URLSearchParams로 안전하게 인코딩하고 검색 계산에서만 trim한다. tag는 정규화된 실제 태그명, sort는 relevance/new만 허용한다. 빈 q, 전체 태그, 기본 정렬은 URL에서 생략한다. 한글·공백·+·&·#·슬래시·퍼센트 문자를 왕복해도 원문이 보존되어야 한다.
- 초기화 순서: 색인 로딩 전에는 tag를 존재하지 않는 값으로 판정하거나 URL에서 삭제하지 않는다. 로딩 성공 후 태그 목록과 대조하고 알 수 없는 태그·정렬만 기본값으로 복구한다. 색인 실패 안내와 재시도 가능성은 기존 동작을 보존한다.
- 이력 기준: 문자마다 push하지 않는다. 연속 입력은 replace로 반영하고 태그·정렬 같은 명시적 선택은 의미 있는 이력 한 번으로 남기는 방식을 우선 검토한다. 뒤로/앞으로에서 q/tag/sort와 화면이 함께 복원되어야 한다. 예약된 URL 갱신이 이동 후 이전 조건을 덮어쓰면 안 된다. 조합 중 한글 입력과 빠른 연속 변경을 검증한다.
- 회귀 시나리오: q=cloud/tag=Cloud Native/sort=new 설정→새로고침→동일 결과, URL을 새 탭에서 열기→동일 조건, 태그·정렬 변경→뒤로/앞으로, 빈 검색과 결과 없음, 잘못된 태그·sort, 한글·특수문자, 느린 색인 로딩 중 초기 태그 보존, 입력 후 즉시 이동. 검색 강조 `AI a`와 320/390/768/1280px 기존 카드 회귀를 함께 검사한다.
- 구현 후보 파일: app/search/SearchClient.tsx 및 순수 URL 직렬화/해석 유틸과 테스트. 정적 export·기존 `/search/?q=…` 진입을 유지한다. 위 기준은 준비 문서이며 아직 기능에 적용하지 않았다.


### SEARCH-004 · 검색 결과가 없을 때 탐색 복구
- 우선순위: 보통 / 상태: 준비.
- 근거·현재 경험: SearchClient의 결과 없음 화면은 한 문장만 표시하며 다음 행동을 제공하지 않는다.
- 대상 독자·개선 후 경험: 태그를 좁히거나 복합 검색어를 입력한 독자가 태그 해제·검색어 수정·주제 목록 이동으로 탐색을 이어간다.
- 예상 효과: 빈 결과에서 탐색 중단 감소. 이용 지표로 측정한 성과는 아니다.
- 수용 기준: 활성 태그가 있을 때 명시적 해제, 입력값 유지, 결과가 없다는 안내와 관련 탐색 경로, 키보드·모바일 접근, 임의로 검색 조건을 바꾸지 않기. 없는 검색 결과나 인기 검색어를 만들어 표시하지 않는다.
- 의존성: SEARCH-002 URL 동기화를 사용해 조건 변경과 뒤로 이동을 보존한다.

### 2026-09-23 SEARCH-002 구현
- 상태: 완료 — 7ab97b5 공개 배포 및 브라우저 회귀 통과. URL을 검색 조건의 기준으로 사용하며 검색어는 replace, 태그·정렬은 push로 기록한다. 잘못된 값은 화면에서 기본값으로 해석하고 다음 명시적 조작 때 주소에서 정리한다.
- 색인 로딩·실패 중에는 태그를 보존한다. useSyncExternalStore로 주소 변경을 동기적으로 반영하고 Next의 useSearchParams와 함께 경로 전환을 추적한다. 입력마다 이력을 추가하거나 지연 타이머를 만들지 않는다.
- 완료 후에도 READ-003, CONTENT-002, DISCOVER-002, DISCOVER-003, SEARCH-004의 미완료 후보 5개를 유지한다.


### 2026-09-24 DISCOVER-002 구현 기준 구체화

- 공개 `/tags/`에 태그 링크 469개, 검색 입력 0개를 확인했다. 390px 화면에는 가로 넘침이 없지만 관심 태그를 찾으려면 긴 목록을 훑어야 한다. Cloud Native는 5편, CI/CD는 2편이다. 이는 콘텐츠 개수이며 독자 이용 지표가 아니다.
- 배치: 제목 소개 아래, 전체 목록 위에 명시적 이름 검색 라벨·검색 입력·초기화 버튼·결과 수를 제공한다. 입력은 태그 이름 검색이며 글 본문 통합 검색과 구분한다.
- 검색 규칙: 이름의 부분 일치, 대소문자 무시, 입력 앞뒤 공백 제거 및 연속 공백 정규화. 공백만 입력하면 전체 목록. Unicode NFC로 한글 조합 표현을 맞춘다. 슬래시·+·점 등은 정규식이 아닌 일반 문자로 비교한다. 기존 태그명·표시 순서·글 수·URL은 보존한다.
- 상태: `전체 469개 중 N개`처럼 결과 수를 알리고 결과 없음에는 검색어 수정 및 초기화를 제공한다. 초기화 후 입력에 포커스를 유지하거나 돌려준다. 키보드만으로 입력→초기화→태그 링크 이동이 가능해야 한다. 매 입력마다 강제 스크롤하지 않는다.
- 렌더링: 서버가 기존 전체 태그 링크를 출력하고 클라이언트에서 필터링한다. JavaScript가 없어도 전체 목록을 읽고 이동할 수 있어야 한다. 동작하지 않는 검색 입력은 JS 준비 전 숨기거나 비활성화하고, 초기화 시 입력 유실이 없도록 검사한다. 태그 이름 검색에 본문 전체 검색 인덱스를 추가로 로드하지 않는다.
- 검증: cloud/CLOUD/앞뒤 공백, Cloud Native 다중 공백, CI/CD, 빈 입력, 결과 없음→초기화, 빠른 입력, 390/1280px 줄바꿈·키보드·실제 링크 클릭, JS 없는 HTML의 링크 개수·주소 보존. 공개 태그에는 한글 이름이 없으므로 한글/NFC·긴 문자열은 테스트 데이터로 검사하고 실제 콘텐츠 검증과 구분한다.
- 범위: 태그 목록 자체의 검색에 한정한다. 정렬 변경·태그 병합·URL 변경·개인화·이용 통계는 포함하지 않는다. 현재 준비 상태이며 아직 구현하지 않았다.


### 2026-09-26 SEARCH-004 수용 기준 구체화와 미완료 확인

- 공개 재현: `/search/?q=cloud&tag=Rust&sort=new`는 결과 0개이며 안내 문장만 표시한다. 태그 선택을 전체로 바꾸면 검색어 cloud와 최신순을 유지한 채 결과 31개로 복구된다. 390px에서 직접 입력 조건과 선택 조작을 확인했다. 이는 검색 오류가 아니라 다음 행동을 찾기 어려운 경험이다.
- 제안 화면: 결과 없음 안내 아래 활성 태그가 있을 때만 `태그 해제하고 다시 찾기` 버튼, 검색어 입력으로 포커스를 돌리는 `검색어 수정` 버튼, `/tags/`로 이동하는 `주제 목록 보기` 링크를 제공한다. 인기 검색어·추천 성과를 추정해 표시하지 않는다.
- 상태 계약: 태그 해제는 기존 changeConditions의 명시적 선택 경로를 사용해 tag만 제거하고 q·sort·기타 URL 조건을 보존한다. 이력을 한 번 추가하고 뒤로 가면 기존 태그와 빈 결과를 복원한다. 검색어 수정은 값을 지우지 않고 포커스만 이동한다. 주제 목록에서 뒤로 돌아오면 이전 검색 조건을 복원한다.
- 수용 기준: cloud/Rust/new의 0→31개 복구, 뒤로/앞으로 복원, 전체 태그에서도 일치하지 않는 검색어, 빈 검색어·로딩·색인 실패 상태와 빈 결과 구분, 빠른 입력 중 조건 유실 없음, 버튼의 Enter/Space 조작, 390/1280px 줄바꿈·포커스 표시를 검증한다. 검색어를 DOM에 표시할 때 일반 텍스트로 렌더링한다.
- 의존성·범위: 완료된 SEARCH-002의 URL 계약을 재사용한다. 새 검색 엔진, 자동 검색어 교정, 추가 데이터 수집은 필요하지 않다. 기존 검색 URL 회귀 검사를 유지하고 복구 동작을 추가 검증한다. 효과는 탐색 지속 편의에 대한 가설이며 실제 전환 수치는 없다.
- DISCOVER-002는 아직 미완료다. 공개 `/tags/`에는 링크 469개와 입력 0개이며 origin/main은 c93f4f0이다. 9월 25일 worktree의 app/tags/page.tsx, src/components/TagSearch.tsx, src/lib/tag-search.ts, src/lib/tag-search.test.ts 및 생성 게임 파일이 미커밋 상태로 남아 있어 이번 실행에서 수정·staging하지 않았다. 다음 기능 회차에 변경 소유권·최신 main과의 차이를 확인한 뒤 검증·배포를 마쳐야 한다.
- 미완료 후보는 READ-003, CONTENT-002, DISCOVER-002, DISCOVER-003, SEARCH-004의 5개다. 태그 검색 배포 완료를 우선하고 SEARCH-004를 후속 후보로 유지한다. 오늘은 설계 구체화이며 기능 완료가 아니다.
